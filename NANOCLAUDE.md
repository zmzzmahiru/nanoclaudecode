# NanoClaude Project Rules

- Keep the framework small, readable, and educational.
- Prefer simple TypeScript modules over large abstractions.
- Preserve the JSON protocol unless a task explicitly asks to change it.
- Keep tools safe by default: validate paths, ask for approval before side effects, and cap large outputs.
- Run `npm run build` after code changes.
