Add a `--shout` flag to the tiny CLI.

Expected behavior:

- `node cli.js --name Ada` prints `Hello, Ada`
- `node cli.js --name Ada --shout` prints `HELLO, ADA`

Make the smallest safe edit and let verification run.
