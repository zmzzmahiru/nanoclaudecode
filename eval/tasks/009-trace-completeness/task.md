Change `src/message.js` so the exported message is `trace-ready`.

Let the configured after-edit verification run. The checker will inspect the
saved NanoClaude session trace for the expected event types.
